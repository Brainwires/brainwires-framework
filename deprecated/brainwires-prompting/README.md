# brainwires-prompting (DEPRECATED)

This crate has been merged into [`brainwires-cognition`](https://crates.io/crates/brainwires-cognition).

Replace in your `Cargo.toml`:

```toml
# Before
brainwires-prompting = "0.2"

# After
brainwires-cognition = { version = "0.3", features = ["prompting"] }
```
