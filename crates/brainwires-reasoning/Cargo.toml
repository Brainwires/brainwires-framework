[package]
name = "brainwires-reasoning"
description = "Reasoning components for the Brainwires Agent Framework"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
brainwires-core = { workspace = true }
brainwires-tooling = { workspace = true, optional = true }
tracing = { workspace = true }
serde = { workspace = true }
regex = { workspace = true }

[features]
default = ["native"]
native = ["brainwires-core/native", "dep:brainwires-tooling", "brainwires-tooling/native"]

[dev-dependencies]
serde_json = { workspace = true }
